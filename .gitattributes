# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Player-submitted replay evidence is binary and retained through Git LFS.
feedback/**/*.gdr filter=lfs diff=lfs merge=lfs -text
